# apify 4.x pulls crawlee 1.x, which is compatible with current pydantic.
# Do NOT loosen this to <3.0.0: apify 2.x drags in crawlee 0.6.x + browserforge,
# which fails at import with "cannot specify both default and default_factory"
# against pydantic >= 2.12.
apify>=4.0,<5.0
httpx>=0.27